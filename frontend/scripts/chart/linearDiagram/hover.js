import { getLine } from "../shapes"

//This script sets up a trigger box for a tooltip

export const getVerticalLine = (data) => {
    const xRange =
    Math.max(...data.map((d) => d["index"])) -
    Math.min(...data.map((d) => d["index"]));


    ЯЯ


    let verticalLine = getLine(0, 0, 0, 512)

    
    
}